# CCT-ASE Python Package

This package contains the Python interface and numerical baseline for the Chrono-Causal Adaptive Spectral Engine research prototype. The optional Rust extension is built from `../causa_core` through the repository-level `Makefile`.
